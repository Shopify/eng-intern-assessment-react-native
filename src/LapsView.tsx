import { TimerState } from "../App";

export default function ScrollViewLaps(
    timerData: TimerState, 
    data: number[], 
) {  
    // First row should be laps w/ the timer component counting up
    // All other rows should be the lap number and the lap time
}

